en:
  activerecord:
    errors:
      messages:
        invalid_transition: "cannot transition"
  activemodel:
    errors:
      messages:
        invalid_transition: "cannot %{event}"
  mongo_mapper:
    errors:
      messages:
        invalid_transition: "cannot transition"
